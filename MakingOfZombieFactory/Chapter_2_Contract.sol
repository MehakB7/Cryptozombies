/***
 * 
 * Chapter 2 : What we learn
 * 1. about the pragma mark which tells about the compiler
 * version should be used to compile contract
 * 2. to create contract use the contract keyword similar to class
 * contract contractName
 * 
 * in solidity we have to add ; after statement else gives error 
 * 
 */

pragma >=0.5.0 <0.6.0.;
contract ZombieFactory{}